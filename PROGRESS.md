# Progress Notes

Running log of milestones, decisions, and fixes for the drone soccer striker
task. Chronological within each section; newest at the bottom.

## Model lineage

| Model | Steps | Env version | Notes |
|---|---|---|---|
| `v1` | 1M (fresh) | old, single opponent | First baseline. |
| `v2` | +2M (from v1), 3M total | old, single opponent | Reached ~69% scored. **Incompatible with current env** — obs space was 14-dim (1 opponent) vs current 26-dim (4 opponents), so it can no longer be loaded against `drone_soccer_env.py`. |
| — | — | env rewrite | Added goalkeeper + 3 random-flying opponents (`N_OPPONENTS=4`), reworked drone mass/thrust-to-weight to DJI Tello specs, added potential-based progress shaping, loosened `MAX_TILT_RAD`. |
| `v3` | 1M (fresh) | current (4 opponents) | "Baseline" in the report. Full-run breakdown (4831 episodes, from recovered `tb_logs/Baseline`): flipped 82.8%, missed_goal 10.0%, out_of_bounds 3.7%, crashed 2.6%, scored 1.0%. Shot accuracy 9.2%. No reward-shaping fixes yet — flip dominates since it wasn't penalized. |
| `v4` | +4M (from v3), 5M total | current | See fixes below. Final breakdown (last ~1M steps, 3125 episodes): missed_goal 52%, flipped 37%, out_of_bounds 6%, scored 5%, crashed 0.4%. **Confirmed fine as-is** — matched 150-episode eval against its own `EvalCallback` best (`checkpoints/best_model_continued/best_model.zip`) showed tied scoring (8.7% both) with the final save actually having *better* shot accuracy (25.0% vs 14.1%). No change needed. |
| `v5` | +4M (from v4), 9M total | current + lateral shaping | Final breakdown (last ~1M steps, 3638 episodes): missed_goal 56%, flipped 32%, scored 12%, out_of_bounds 0.2%, crashed 0%. Raw scored% roughly doubled vs v4 (4.7%→11.6%); shot accuracy scored/(scored+missed) also roughly doubled (~8.3%→~17.1%). `std` continued sharpening 0.7→0.457, no runaway. **Confirmed fine as-is** — matched eval against its own `EvalCallback` best (`checkpoints/best_model_continued_v4/best_model.zip`) showed the final save *better* (11.3% vs 9.3% scored) — the callback's noisy 10-episode checks didn't catch the true best here. No change needed. |
| `v6` (final save, 13M) | +4M (from v5) | current + lateral shaping, `device="cpu"` | **Regressed, not improved** — the raw final save. Matched 150-episode eval: only 8.7% scored, 12.4% shot accuracy. Same non-monotonic PPO pattern as `v7`. **Not used as the Stage 3 result** — see `v6_best` below. First run trained on `device="cpu"` (see hardware notes) — resumed from a mid-run checkpoint (11.21M steps) rather than restarting, since the only thing wrong with the original run was the device, not the training itself. |
| `v6_best` (mid-run checkpoint) | mid-`v6`-run checkpoint | current + lateral shaping | **This is the actual Stage 3 result used in the report**, not the final save above. Caught by `EvalCallback` (`checkpoints/best_model_continued_v5/best_model.zip`, copied to `drone_soccer_ppo_v6_best.zip`). Matched 150-episode eval: 22.7% scored (vs final's 8.7%), 31.8% shot accuracy (vs 12.4%) — more than double on both. |
| `v7` (final save, 17M) | +4M (from v6) | current + lateral shaping (Stage 4, no reward changes) | **Regressed, not improved** — the raw final save. Last ~1M steps, 3473 episodes: scored dropped 14.5%→11.1%, flipped up 30.2%→32.0%, missed_goal ~flat 55.2%→56.3%, out_of_bounds up 0.1%→0.6%. Non-monotonic PPO behavior — policy peaked mid-run and declined over the final ~1M steps. **Not used as the Stage 4 result** — see `v7_best` below. |
| `v7_best` (~16M steps) | mid-`v7`-run checkpoint | current + lateral shaping | **This is the actual Stage 4 result used in the report**, not the final save above, and the **best model overall** across every checkpoint checked. Caught by `EvalCallback` (`checkpoints/best_model_continued_v6/best_model.zip`, saved 09:43, copied to `drone_soccer_ppo_v7_best.zip`). Confirmed genuinely better via a fresh matched 150-episode eval against the final save: scored 27.3% vs 14.0%, flipped 48.0% vs 56.7%, missed_goal 24.7% vs 29.3%. Shot accuracy 52.6% (41/78). Two independent measurements (training-log aggregate and this fresh eval) agree the final save regressed from this point — not sampling noise. |

## Key findings & fixes

- **Flip/miss had no reward penalty.** `_check_done()` could end an episode as `"flipped"` or `"missed_goal"`, but `_compute_reward()` had no matching branch — those outcomes scored the same as an ordinary step, so the policy had no direct incentive to avoid them beyond losing future progress reward. Added `FLIP_PENALTY=-30`, `MISS_PENALTY=-10`.
- **`ent_coef=0.01` caused an entropy runaway once those penalties were added.** Continuing v3→v4 with the old ent_coef, action `std` climbed monotonically from ~1.4 to ~5.5 over 4M steps (never plateaued) — the entropy bonus outweighed the policy-gradient signal while the value function was still adjusting to the new reward scale. With actions clipped to `[-1, 1]`, that std means most sampled actions are just noise slammed against the clip boundary. Fixed by lowering to `ent_coef=0.001`; re-ran v3→v4 clean (std settled to ~0.7).
- **`out_of_bounds` has the same missing-penalty gap** as flip/miss did — not yet fixed, and crept up to 6% in v4's final stretch. Candidate follow-up.
- **Flip mechanics diagnostic** (60 headless episodes against v4): even non-flip episodes reach ~80° tilt on average (task requires very aggressive flight near the tilt limit as normal operation, not just in failure cases). In the last 15 steps before an actual flip, angular velocity *accelerates* (7.5 → 14 rad/s) while action magnitude stays high/near-saturated the whole time — the policy is actively fighting the spin, not ignoring it; available roll/pitch torque authority (`ATTITUDE_GAIN=0.4`) just isn't enough to arrest it once started. Suggests raising `ATTITUDE_GAIN` or `angularDamping` as a control-side fix, separate from reward tuning. **Not yet implemented** — deprioritized after the large-sample data below showed missing, not flipping, is the bigger failure mode.
- **Large-sample check (3125 episodes) showed `missed_goal` (52%) is the dominant failure mode, not `flipped` (37%)** — the 60-episode diagnostic above was noisy on this point. Root cause: `PROGRESS_SCALE` shapes toward raw 3D distance to `goal_pos`, which doesn't specifically reward staying centered on the hoop's Y-Z opening — a drone can reduce net distance while drifting off-axis and only find out at the goal plane.
- **Final-approach lateral shaping added** to address the above: `LATERAL_SCALE=10.0` potential-based reward for reducing Y-Z distance to the hoop center, gated to `drone_pos[0] >= FINAL_APPROACH_X (2.0)` so it doesn't fight goalkeeper-dodging earlier in the flight. Result (v4→v5, 4M steps): worked, but not obviously so from the raw `missed_goal` % alone (52%→56%, looks flat/worse) — the useful read is conditional shot accuracy, `scored/(scored+missed_goal)`, which roughly doubled (~8.3%→~17.1%). Raw `missed_goal`% rose because `flipped`/`out_of_bounds`/`crashed` all dropped, funneling more episodes into "reached the goal plane" at all, and only part of that larger pool converts to scores yet. Lesson: don't read `missed_goal`% in isolation when other failure modes are shifting at the same time.
- **Checkpoint/tensorboard naming collisions**, twice: `train_continue.py`'s `CheckpointCallback` name_prefix and `tb_log_name` need to be unique per lineage (`v3_continued`, `v4_continued`, ...) — `reset_num_timesteps=False` makes SB3 reuse the *latest existing* run directory for a given name rather than incrementing, so reusing a name across different training lineages silently interleaves unrelated runs' curves or overwrites old checkpoint files at the same step numbers.
- **PPO's non-monotonic regression hit `v6` and `v7`, but not `v4`/`v5`.** Checked every stage's final save against its own `EvalCallback`-picked best checkpoint with a matched 150-episode eval: `v4` and `v5`'s final saves were fine (tied or better than their "best" pick — the callback's noisy 10-episode checks aren't always right either). `v6` and `v7` had genuinely regressed by their final save (more than 2x worse scoring than their mid-run peaks). Lesson: this has to be checked per-run, not assumed either way — neither "the final save is always right" nor "always distrust the final save" holds. `v6_best`/`v7_best` (copied from their respective `checkpoints/best_model_continued_v*/best_model.zip`) are the correct Stage 3/4 results, not the raw final saves. **`v7_best` is the best model found overall.**

## Hardware / throughput notes

- Training is CPU-bound (PyBullet physics stepped across `SubprocVecEnv` workers), not GPU-bound — the policy net (tiny MLP+LSTM) barely uses the GPU (~13% util observed).
- Current machine: Ryzen 5 7500F (6 cores/12 threads). `N_ENVS` raised from 8 → 10 (oversubscribing SMT threads, leaving 2 free for main process/OS) — safe and effective in practice.
- If moving to a Ryzen 7 9700X (8 cores/16 threads): recommend `N_ENVS≈13-14` (same oversubscription ratio), not just matching physical core count. Estimated ~5-5.5h for a run that takes 8h here (more parallel envs + faster per-core Zen5 performance).
- **Task Manager showing only ~36% CPU utilization during training led to a deeper look.** Initial hypothesis (SubprocVecEnv's Windows IPC overhead dominating) was wrong: a side-by-side benchmark of `DummyVecEnv` vs `SubprocVecEnv` inside the real training loop showed no difference (1.05x) — both settle to the same ~120-140 fps. The actual bottleneck is the **PPO update phase** (10 epochs x ~20 minibatches per rollout, LSTM forward+backward through each), not environment stepping — during that phase only the main process works while all env-worker processes idle, which is what shows up as low utilization. Confirmed fix: **`device="cpu"` beats the default `"auto"`/cuda by 1.29x** (130.6 vs 101.4 fps) for this tiny policy — GPU kernel-launch/PCIe-transfer overhead per minibatch exceeds the compute it saves. Applied to `train.py`/`train_continue.py`. Lesson: a raw single-env stepping benchmark without policy inference/gradient updates is not representative of real training throughput — measure the actual loop.
- **`tb_logs/` cleaned up and relabeled to match the report's Stage 1/2/3 naming** (see model lineage table): `RecurrentPPO_v3_continued_0` (v3→v4) → `Stage_1`, `RecurrentPPO_v4_continued_0` (v4→v5) → `Stage_2`, `RecurrentPPO_v5_continued_0` (v5→v6) → `Stage_3`. Everything else initially removed — `RecurrentPPO_1-7` (pre-report prototyping and v1/v3's own fresh-start curves) and `RecurrentPPO_continued_0` (old v1→v2 run under the obsolete single-opponent env). If continuing training past `v6`, keep using this Stage naming for any new tensorboard runs to stay consistent with the report.
- **`RecurrentPPO_7` (v3's own fresh-start training curve) was deleted too broadly in that cleanup, then recovered from git history** — commit `9d16388` ("add more complexity to the env") had committed it before deletion, since PID `11892` in its filename matched the known `train.py` process that produced `v3.zip`. Restored as `tb_logs/Baseline` (245 points, steps 4,096-1,003,520, matches v3's known training window exactly). `RecurrentPPO_1-6` and `RecurrentPPO_continued_0` were *not* recoverable this way — they were never committed, so if needed again they'd have to come from OneDrive's cloud recycle bin (this whole project lives inside a OneDrive-synced folder) rather than git.

## Open items / next steps

- `out_of_bounds` still has no explicit penalty (negligible in the good checkpoints though).
- Flip-recovery control authority (`ATTITUDE_GAIN`/`angularDamping`) not yet tuned — flip rate still ~28-48% depending on checkpoint. Worth trying next, one variable at a time (raise `ATTITUDE_GAIN` OR `angularDamping`, not both, so the effect is attributable).
- `v7_best` (27.3% scored, 52.6% shot accuracy) is the best model so far, but any future continuation should start from `v7_best.zip`, not the raw `v7.zip` final save — and should re-check the resulting run's final save against its own `EvalCallback` best before trusting it, given the mixed pattern above.
- Detector is still `None` (ground-truth opponent positions) — swapping in a real detector later will introduce observation noise the current policy has never trained against; expect a regression when that happens.
